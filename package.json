{
  "name": "json-explorer-viewer",
  "displayName": "JSON Explorer",
  "description": "Visualize JSON files as an interactive Tree, Column, or Key-Value/Table view, right beside the source editor.",
  "version": "0.2.0",
  "publisher": "sohampatwardhan",
  "license": "MIT",
  "icon": "media/icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/sohampatwardhan/json-explorer-vscode.git"
  },
  "engines": {
    "vscode": "^1.125.0"
  },
  "main": "./dist/extension.js",
  "categories": [
    "Visualization",
    "Other"
  ],
  "activationEvents": [
    "onCommand:jsonExplorer.visualize",
    "onCommand:jsonTables.visualize"
  ],
  "contributes": {
    "commands": [
      {
        "command": "jsonExplorer.visualize",
        "title": "Visualize JSON",
        "category": "JSON Explorer",
        "icon": "$(json)"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "jsonExplorer.visualize",
          "when": "resourceLangId == json || resourceLangId == jsonc",
          "group": "navigation"
        }
      ]
    }
  },
  "scripts": {
    "compile": "node esbuild.config.mjs",
    "watch": "node esbuild.config.mjs --watch",
    "test": "node --import tsx --test \"src/**/*.test.ts\" \"src/**/*.test.tsx\"",
    "package": "npm run compile -- --production && vsce package",
    "publish": "npm run compile -- --production && vsce publish"
  },
  "dependencies": {
    "jsonc-parser": "3.3.1",
    "preact": "10.29.8"
  },
  "devDependencies": {
    "@testing-library/preact": "3.2.4",
    "@types/jsdom": "^30.0.0",
    "@types/node": "26.2.0",
    "@types/vscode": "1.125.0",
    "@vscode/vsce": "3.9.2",
    "esbuild": "0.28.2",
    "jsdom": "30.0.1",
    "tsx": "4.23.11",
    "typescript": "7.0.2"
  }
}
